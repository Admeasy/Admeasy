# Backend API URL
VITE_API_URL=http://localhost:5000

# Google Web client ID for native sign-in is hardcoded in src/auth/googleSignIn.ts (public OAuth client id).
# Server still uses GOOGLE_CLIENT_ID in .env — must match that string.