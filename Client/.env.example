# Backend API URL
VITE_API_URL=http://localhost:5000

# Google OAuth — Web client ID (same as Server GOOGLE_CLIENT_ID). Required for native Android Google Sign-In.
# Also set the same value as plugins.GoogleAuth.serverClientId in capacitor.config.json before `npx cap sync android`.
VITE_GOOGLE_WEB_CLIENT_ID=131243298453-f8l1eud7gadl0mgap85smr5le64g7k95.apps.googleusercontent.com